import { MembersView } from '@/components/features/organization/members-view';

export const metadata = {
  title: 'Organization Members & Roles | Gami Community Engine',
  description: 'Manage organization team members, roles, and permissions.',
};

export default function MembersPage() {
  return <MembersView />;
}
